# Power Automate webhook URL for posting module completion data
VITE_POWERAUTOMATE_URL=

# Admin password for Edit Mode (client-side only — not a security boundary)
VITE_ADMIN_PASSWORD=

# Set to your backend API base URL in production.
# Leave blank to use local data files.
VITE_API_URL=
